<Page
    x:Class="Kagic_UI.Views.Game"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kagic_UI.Views"
    xmlns:vm="using:Kagic_UI.ViewModels"
    xmlns:dts="using:Kagic_UI.Models.Utilities"
    xmlns:numberConverter="using:Kagic_UI.ViewModels.UtilitiesVM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:VMFight/>
    </Page.DataContext>

    <Page.Resources>

        <numberConverter:clsTensNumberConverter x:Key="tensNumConv"/>
        <numberConverter:clsUnitNumberConverter x:Key="unitNumConv"/>

        <!-- Templates -->
        <DataTemplate x:Key="dtCreature">
            <RelativePanel Style="{StaticResource RpCardSmall}">
                <Image Name="image"     Source="{Binding Image}"                                                                                    Style="{StaticResource ImgCardImageSmall}"/>
                <Image Name="frame"     Source="/Assets/Images/CreatureFrame.png"                                                                   Style="{StaticResource ImgCardImageSmall}"/>
                <Image Name="numAttack" Source="{Binding Attack, Converter={StaticResource unitNumConv}}"                                           Style="{StaticResource ImgAttackSmall}" Canvas.ZIndex="4"/>
                <Image Name="attack"    Source="/Assets/Images/Attack.png"                                                                          Style="{StaticResource ImgAttackSmall}"/>
                <Image Name="numLife"   Source="{Binding ActualLife, Converter={StaticResource unitNumConv}, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource ImgLifeSmall}" Canvas.ZIndex="4"/>
                <Image Name="Life"      Source="/Assets/Images/Life.png"                                                                            Style="{StaticResource ImgLifeSmall}"/>
                <Image Name="numMana"   Source="{Binding Manacost, Converter={StaticResource unitNumConv}}"                                         Style="{StaticResource ImgCardManaCostSmall}" Canvas.ZIndex="4"/>
                <Image Name="mana"      Source="/Assets/Images/Mana.png"                                                                            Style="{StaticResource ImgCardManaCostSmallManaImg}"/>
                <TextBlock Name="name"  Text="{Binding Name}" Canvas.ZIndex="4"  Foreground="Black" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignBottomWithPanel="True" Margin="0,0,0,104" FontWeight="SemiBold" FontStyle="Italic"/>
                <TextBlock Name="description" Text="{Binding Description}" Canvas.ZIndex="4" Foreground="Black" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignTopWithPanel="True" Margin="0,130,0,0" MaxWidth="125" MaxHeight="300" MaxLines="3" TextWrapping="Wrap"/>
            </RelativePanel>
        </DataTemplate>

        <DataTemplate x:Key="dtHealingSpell">
            <RelativePanel Style="{StaticResource RpCardSmall}">
                <Image Name="image"     Source="{Binding Image}"                                            Style="{StaticResource ImgCardImageSmall}"/>
                <Image Name="frame"     Source="/Assets/Images/SpellFrame.png"                              Style="{StaticResource ImgCardImageSmall}"/>
                <Image Name="numEffect" Source="{Binding Effect, Converter={StaticResource unitNumConv}}"   Style="{StaticResource ImgLifeSmall}" Canvas.ZIndex="4"/>
                <Image Name="cure"      Source="/Assets/Images/Cure.png"                                    Style="{StaticResource ImgLifeSmall}"/>
                <Image Name="numMana"   Source="{Binding Manacost, Converter={StaticResource unitNumConv}}" Style="{StaticResource ImgCardManaCostSmall}" Canvas.ZIndex="4"/>
                <Image Name="mana"      Source="/Assets/Images/Mana.png"                                    Style="{StaticResource ImgCardManaCostSmallManaImg}"/>
                <TextBlock Name="name"  Text="{Binding Name}" Canvas.ZIndex="4"  Foreground="Black" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignBottomWithPanel="True" Margin="0,0,0,104" FontWeight="SemiBold" FontStyle="Italic"/>
                <TextBlock Name="description" Text="{Binding Description}" Canvas.ZIndex="4" Foreground="Black" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignTopWithPanel="True" Margin="0,130,0,0" MaxWidth="125" MaxHeight="300" MaxLines="3" TextWrapping="Wrap"/>
                <TextBlock Name="area" Text="AREA" FontWeight="Bold" Canvas.ZIndex="4" RelativePanel.AlignTopWith="numEffect" RelativePanel.AlignHorizontalCenterWithPanel="True"  Visibility="{Binding IsArea}"/>
            </RelativePanel>
        </DataTemplate>

        <DataTemplate x:Key="dtDamageSpell">
            <RelativePanel Style="{StaticResource RpCardSmall}">
                <Image Name="image"     Source="{Binding Image}"                                            Style="{StaticResource ImgCardImageSmall}"/>
                <Image Name="frame"     Source="/Assets/Images/SpellFrame.png"                              Style="{StaticResource ImgCardImageSmall}"/>
                <Image Name="numEffect" Source="{Binding Effect, Converter={StaticResource unitNumConv}}"   Style="{StaticResource ImgLifeSmall}" Canvas.ZIndex="4"/>
                <Image Name="damage"    Source="/Assets/Images/Damage.png"                                  Style="{StaticResource ImgLifeSmall}"/>
                <Image Name="numMana"   Source="{Binding Manacost, Converter={StaticResource unitNumConv}}" Style="{StaticResource ImgCardManaCostSmall}" Canvas.ZIndex="4"/>
                <Image Name="mana"      Source="/Assets/Images/Mana.png"                                    Style="{StaticResource ImgCardManaCostSmallManaImg}"/>
                <TextBlock Name="name"  Text="{Binding Name}" Canvas.ZIndex="4" Foreground="Black" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignBottomWithPanel="True" Margin="0,0,0,104" FontWeight="SemiBold" FontStyle="Italic"/>
                <TextBlock Name="description" Text="{Binding Description}" Canvas.ZIndex="4" Foreground="Black" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignTopWithPanel="True" Margin="0,130,0,0" MaxWidth="125" MaxHeight="300" MaxLines="3" TextWrapping="Wrap"/>
                <TextBlock Name="area" Text="AREA" FontWeight="Bold" Canvas.ZIndex="4" RelativePanel.AlignTopWith="numEffect" RelativePanel.AlignHorizontalCenterWithPanel="True"  Visibility="{Binding IsArea}"/>
            </RelativePanel>
        </DataTemplate>
        
        <DataTemplate x:Key="dtEmpty">
            <RelativePanel>
                <Image Name="frame" Source="/Assets/Images/CardPosition.png" Style="{StaticResource ImgCardImageSmall}" Opacity="0.2"/>
            </RelativePanel>
        </DataTemplate>

        <dts:MyDataTemplateSelector x:Key="CardsTemplates" Creature="{StaticResource dtCreature}" DamageSpell="{StaticResource dtDamageSpell}" HealingSpell="{StaticResource dtHealingSpell}"/>
        <dts:BattlefieldDataTemplateSelector x:Key="BattlefieldCardsTemplates" Creature="{StaticResource dtCreature}" Empty="{StaticResource dtEmpty}"/>
    </Page.Resources>

    <!-- Background -->
    <Page.Background>
        <ImageBrush ImageSource="/Assets/Images/Background.png"/>
    </Page.Background>

    <RelativePanel>


        <!-- Back button -->
        <AppBarButton x:Name="btnBack" Icon="Back" Foreground="Black" Margin="5" VerticalAlignment="Center" Click="AppBarButton_Click" Background="White" CornerRadius="10"/>

        <!-- IA Cards -->
        <GridView x:Name="gdOpponentCards" Height="150" MinWidth="400" ItemsSource="{Binding IaPlayer.Hand, UpdateSourceTrigger=PropertyChanged}" 
            VerticalContentAlignment="Bottom" ScrollViewer.VerticalScrollBarVisibility="Disabled"
            RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True" IsItemClickEnabled="False" SelectionMode="None" SelectedIndex="{Binding IaPlayer.SelectedCard, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
            <GridView.ItemTemplate>
                <DataTemplate>
                    <RelativePanel Style="{StaticResource RpCardSmall}" VerticalAlignment="Bottom">
                        <Canvas>
                            <Image VerticalAlignment="top" Source="/Assets/Images/BackImage.png" MaxHeight="250" MaxWidth="150" Canvas.Top="-100"
                                RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
                        </Canvas>
                    </RelativePanel>
                </DataTemplate>
            </GridView.ItemTemplate>

        </GridView>


        <!-- Battlefield -->
        
        <RelativePanel x:Name="rpBattlefield" Margin="0,30,0,0" BorderThickness="3" Padding="5" Background="White" CornerRadius="20"
                       RelativePanel.Below="gdOpponentCards" RelativePanel.AlignHorizontalCenterWithPanel="True" >
            <!-- IA battlefield -->
            <GridView x:Name="gdOpponentCreatures" ItemsSource="{Binding IaPlayer.PlaceCreatures, UpdateSourceTrigger=PropertyChanged}" ItemTemplateSelector="{StaticResource BattlefieldCardsTemplates}"  MinHeight="250" MinWidth="800" Margin="0,0,0,20" BorderThickness="3" Background="Red" CornerRadius="20"
                      RelativePanel.AlignHorizontalCenterWithPanel="True"                                           SelectedIndex="{Binding IaPlayer.SelectedCreature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedCreature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single" SingleSelectionFollowsFocus="True">
            </GridView>
    

            <!-- Player battlefield -->
            <GridView x:Name="gdPlayerCreatures" ItemsSource="{Binding RealPlayer.PlaceCreatures, UpdateSourceTrigger=PropertyChanged}" ItemTemplateSelector="{StaticResource BattlefieldCardsTemplates}" MinHeight="250" MinWidth="800" BorderThickness="3" Background="Blue" CornerRadius="20"
                      RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="gdOpponentCreatures" SelectedIndex="{Binding RealPlayer.SelectedCreature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedCreature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
            </GridView>
        </RelativePanel>
    

        <!-- Next turn button -->
        <Button x:Name="btnNextTurn" Content="Next Turn" Margin="10" Background="White" CornerRadius="10"
                RelativePanel.AlignVerticalCenterWith="rpBattlefield" RelativePanel.AlignLeftWithPanel="True" Command="{Binding PassTurnCommand}"/>
        <!--Cambiar por user control con bindeos y tal-->
        <!-- IA life -->
        <RelativePanel x:Name="stpIaLife" Style="{StaticResource normalLife}" 
                    RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignTopWithPanel="True">
            <ProgressBar x:Name="pgbIaLife" Height="20" Width="150" Value="{Binding IaPlayer.ProgresBarLife, UpdateSourceTrigger=PropertyChanged}" Foreground="Red" Background="LightGray"
                     RelativePanel.AlignBottomWithPanel="True"/>
            <Image x:Name="imgIaLife"           Source="/Assets/Images/Life.png"    Height="150"    Canvas.ZIndex="1" RelativePanel.AlignTopWithPanel="True"/>
            <Image x:Name="imgIaTensNumber"     Source="{Binding IaPlayer.Life, Converter={StaticResource tensNumConv}, UpdateSourceTrigger=PropertyChanged}" Height="80" Canvas.ZIndex="2" Margin="0,20,0,0" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True"/>
            <Image x:Name="imgIaUnitsNumber"    Source="{Binding IaPlayer.Life, Converter={StaticResource unitNumConv}, UpdateSourceTrigger=PropertyChanged}" Height="80" Canvas.ZIndex="2" Margin="0,20,0,0" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
        </RelativePanel>
        
        <!--Boton para atacar al contrario-->
        <Button x:Name="buttonAttackContrary" RelativePanel.Below="stpIaLife" RelativePanel.AlignRightWithPanel="True" Margin="40,10" Background="LightGray" Command="{Binding AttackContraryPlayerCommand}">Atacar contrario</Button>

        <!--Boton para curarte-->
        <Button x:Name="buttonHealthyou" RelativePanel.Above="stpPlayerLife" RelativePanel.AlignLeftWithPanel="True" Margin="40,10" Background="LightGray" Command="{Binding HealthPlayerCommand}">Curar</Button>
        
        <!-- Player life -->
        <RelativePanel x:Name="stpPlayerLife" Style="{StaticResource normalLife}"
                    RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignBottomWithPanel="True">
            <ProgressBar x:Name="pgbPlayerLife" Height="20" Width="150" Value="{Binding RealPlayer.ProgresBarLife, UpdateSourceTrigger=PropertyChanged}" Foreground="DodgerBlue" Background="LightGray"
                     RelativePanel.AlignBottomWithPanel="True"/>
            <Image x:Name="imgPlayerLife"   Source="/Assets/Images/Life.png"    Height="150"    RelativePanel.AlignTopWithPanel="True"/>
            <Image x:Name="imgTensNumber"   Source="{Binding RealPlayer.Life, Converter={StaticResource tensNumConv}, UpdateSourceTrigger=PropertyChanged}" Height="80" Canvas.ZIndex="2" Margin="0,20,0,0" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True"/>
            <Image x:Name="imgUnitsNumber"  Source="{Binding RealPlayer.Life, Converter={StaticResource unitNumConv}, UpdateSourceTrigger=PropertyChanged}" Height="80" Canvas.ZIndex="2" Margin="0,20,0,0" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignRightWithPanel="True"/>
        </RelativePanel>
       
        <!--Cambiar por user control con bindeos y tal-->

        <!-- Card details -->
        <RelativePanel Height="300" Width="200" x:Name="laCartaSeleccionada"
            RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True">
            <GridView x:Name="gdSelectedCard"  ItemsSource="{Binding LastSelectedCard, UpdateSourceTrigger=PropertyChanged}"
            RelativePanel.AlignVerticalCenterWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True" ItemTemplateSelector="{StaticResource CardsTemplates}" Visibility="{Binding CardDetailsVisibility, UpdateSourceTrigger=PropertyChanged}">
            </GridView>
        </RelativePanel>


        <!-- Player Cards -->
        <GridView x:Name="gdPlayerCards" MinHeight="200" MinWidth="800" ItemsSource="{Binding RealPlayer.Hand}"
            RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True" 
            ItemTemplateSelector="{StaticResource CardsTemplates}" SelectedIndex="{Binding RealPlayer.SelectedCard, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedCard, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        </GridView>

        <!--ManaBar-->
        <RelativePanel RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Above="gdPlayerCards" MinWidth="920">
            <RelativePanel x:Name="rpActualMana" RelativePanel.AlignVerticalCenterWith="rpPlayerMana" RelativePanel.AlignLeftWithPanel="True" Margin="5" Canvas.ZIndex="3">
                <Image Source="/Assets/Images/Mana.png" Width="70" Height="70" Canvas.ZIndex="3" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True"/>
                <StackPanel Orientation="Horizontal" Canvas.ZIndex="4" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True">
                    <Image x:Name="imgTensNumberUsedMana"   Source="{Binding RealPlayer.UsedMana, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource tensNumConv}}"           Height="25"/>
                    <Image x:Name="imgUnitsNumberUsedMana"  Source="{Binding RealPlayer.UsedMana, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource unitNumConv}}"           Height="25"/>
                </StackPanel>
            </RelativePanel>

            <StackPanel Orientation="Horizontal" RelativePanel.RightOf="rpActualMana" RelativePanel.Below="rpPlayerMana" Canvas.ZIndex="4">
                <TextBlock Text="Máximo:"                           Style="{StaticResource ManaText}"/>
                <TextBlock Text="{Binding RealPlayer.TotalMana}"    Style="{StaticResource ManaText}" Margin="5,0"/>
            </StackPanel>
            
            <RelativePanel x:Name="rpPlayerMana"  CornerRadius="5" Background="White" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True">
                <ProgressBar Foreground="DarkViolet" CornerRadius="15" Value="{Binding RealPlayer.ProgresBarMana}" Background="LightGray" Height="26" Width="800" Margin="20, 10" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True"/>
                <Image Source="/Assets/Images/ManaBar.png" Width="800" Margin="20, 10" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True" Canvas.ZIndex="2"/>
            </RelativePanel>
        </RelativePanel>

    </RelativePanel>

</Page>